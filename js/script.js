const construccion = document.querySelectorAll(".enconstruccion");
construccion.forEach((punto) => {
    punto.addEventListener("click", () => {
        alert("Todavía no y no pienses en el tema oscuro...");
    });
});